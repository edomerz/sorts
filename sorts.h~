/*
	sorts
*/
	
#ifndef __SORTS_H__
#define __SORTS_H__

#include <stdio.h>

typedef int (*cmp)(const void *data1, const void *data2);

void(InsertionSort(void *base, size_t num_of_elements, size_t element_size, cmp fp);
	
#endif /* ifndef __SORTS_H__*/
